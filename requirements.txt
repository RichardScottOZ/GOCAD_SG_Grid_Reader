# Core dependencies
numpy>=1.20.0

# Optional dependencies for full functionality
pyvista>=0.38.0  # For 3D grid visualization and VTK export

# For Jupyter notebook examples (already in repo)
jupyter>=1.0.0
pandas>=1.3.0
